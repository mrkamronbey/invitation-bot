import type { TemplateMeta } from '../types';
import { FloralTemplate } from './FloralTemplate';

export const floralTemplate: TemplateMeta = {
  id: 'floral',
  name: 'Gulli ramka',
  previewImage: '/images/frame-floral.png',
  component: FloralTemplate,
};
