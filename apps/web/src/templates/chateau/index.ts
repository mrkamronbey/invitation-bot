import type { TemplateMeta } from '../types';
import { ChateauTemplate } from './ChateauTemplate';

export const chateauTemplate: TemplateMeta = {
  id: 'chateau',
  name: 'Chateau 🌿',
  previewImage: '/images/chateau/chateau.webp',
  component: ChateauTemplate,
};
